# Python Environments & Caches
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
.env
.venv312

# AI Artifacts & Memory
# The production vectorstore is a deliberately versioned, runnable snapshot.
# Staging output, backups, and query logs remain machine-local.
artifacts/vectorstore_staging/
artifacts/vectorstore_backup/
artifacts/retrieval_logs/
*.faiss
# The corpus is licensed content (RULE 3.1) and is NOT versioned here.  It
# was, until 2026-09-03, via two "!" negations that used to sit right below
# *.faiss -- that is exactly how 190 MB of Harrison's reached a public repo.
# Do not re-add them.  docs/CORPUS.md says where the corpus lives instead.
artifacts/vectorstore/
!artifacts/vectorstore/README.md
data/

# Visual Assets (Ignores the 8,000+ images)
storage/pages/

# Logs
*.log

# OS Generated Files (Mac/Windows)
.DS_Store
.AppleDouble
.LSOverride
Thumbs.db

# IDEs
.vscode/
.cursor/

*.pdf
artifacts/semantic_cache.json

# Local tool state / benchmark scratch output
.serena/
.playwright-mcp/
artifacts/benchmark/
.claude/settings.local.json
impeccable/
.impeccable/
