# Pinned to the versions the checked-in FAISS index and documented runtime
# (.venv312, Python 3.12) were validated against.  Unpinned, a fresh install
# resolves a different transformers/torch pair and may not reproduce the same
# embeddings against artifacts/vectorstore/index.faiss.
#
# Regenerate with:  .venv312/bin/pip freeze

# --- API & serving ---
fastapi==0.138.1
uvicorn==0.49.0
pydantic==2.13.4
python-dotenv==1.2.2

# --- Vector & lexical search ---
faiss-cpu==1.14.3
rank-bm25==0.2.2
numpy==2.5.0

# --- Embeddings & reranking ---
sentence-transformers==5.6.0
torch==2.12.1
transformers==5.12.1

# --- Deployment ---
# Already present transitively (sentence-transformers requires it); pinning it
# is a reproducibility control, not a new dependency, per the note on pydantic
# and torch above.  scripts/fetch_corpus.py imports it directly to pull the
# licensed corpus from a private HF dataset at container boot, so an unpinned
# resolve could change that code path without touching this file.
huggingface_hub==1.21.0

# --- LLM providers ---
google-genai==2.10.0
groq==1.6.0
